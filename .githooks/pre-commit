#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

ruby_files=()
while IFS= read -r -d '' path; do
  case "$path" in
    *.rb|Gemfile|Rakefile|config.ru)
      ruby_files+=("$path")
      ;;
  esac
done < <(git diff --cached --name-only --diff-filter=ACMR -z)

if [[ "${#ruby_files[@]}" -eq 0 ]]; then
  exit 0
fi

partially_staged=()
while IFS= read -r -d '' path; do
  partially_staged+=("$path")
done < <(git diff --name-only -z -- "${ruby_files[@]}")

if [[ "${#partially_staged[@]}" -gt 0 ]]; then
  printf 'pre-commit: these Ruby files have unstaged changes, refusing to auto-format:\n' >&2
  printf '  %s\n' "${partially_staged[@]}" >&2
  printf 'Stage or stash them first, then commit again.\n' >&2
  exit 1
fi

if [[ ! -x "bin/rubocop" ]]; then
  printf 'pre-commit: bin/rubocop is missing or not executable.\n' >&2
  exit 1
fi

printf 'pre-commit: formatting staged Ruby files with RuboCop...\n'
bin/rubocop -A --force-exclusion "${ruby_files[@]}"
git add -- "${ruby_files[@]}"
